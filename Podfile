# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'blockview' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Anchorage'
  pod 'lottie-ios'
  pod 'QRCode', :git => 'https://github.com/AlexLopezDevelop/QRCode'
  pod 'HydraAsync'

  # Pods for blockview

  target 'blockviewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'blockviewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
